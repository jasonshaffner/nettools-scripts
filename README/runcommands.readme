Runs commands stored in files for IOS, XR and JUNOS, on devices listed in file.

Cut/paste the script into a file in your preferred directory.
Run the script using python, or use "chmod +x" to make the script executable.

Save list of devices which you would like to check in the same directory as the script.
Save list of commands for various platforms you would like to check in the same directory as the script.
File names for commands should be 'IOS', 'XR' and 'JUNOS'.
